# RL Dev Tracker Data

Persistent datasets for the Rocket League dev/activity Discord bot.

- `datasets/users.json` — tracked accounts and alert configuration
- `datasets/activity.json` — observed Tracker.gg activity history

The Hugging Face bot manages these files automatically using its `GHT` secret.
Do not commit the GitHub token or Discord bot token to this repository.
